i = 0
numbers = []
top = 6
step = 1

# while i < top:
	# print "At the top i is %d " % i
	# numbers.append(i)

	# i  += step
	# print "Numbers now:", numbers
	# print "At the bottom i is %d" % i

for i in range(top):
	print "At the top i is %d" % i
	numbers.append(i)
	#i += step
	print "Numbers now:", numbers
	print "At the bottom i is %d" % i
	
print "The numbers"

# for num in numbers:
	# print num

for num in range(top):
	print num